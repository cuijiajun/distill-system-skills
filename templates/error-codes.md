# 错误码与错误处理

> 统一信封：<!-- <结构> -->。失败时 <!-- <字段约定，如 success=false + error.code + error.message> -->。

## errorCode 总览
| errorCode | HTTP | 含义 | 典型来源 |
|-----------|------|------|----------|
<!-- | CONTRACT_NO_DUPLICATE | 409 | 合同编号重复 | create-contract | -->

## 给 AI 的处理建议
1. <!-- <每类错误 AI 该怎么处理/解释/是否重试，例：
   - 校验类错误（比例≠1、必填缺失）→ 不要重试，向用户解释并请求修正。
   - 唯一性冲突 → 提示用户该值已存在，请换一个。
   - 状态非法（对已收款发票重复确认）→ 说明当前状态不允许该操作。> -->

---

> 填写说明：错误消息以**代码实际返回为准**，逐条核对，不要臆造。API 文档出现的每个错误码这里都要能查到。
